{% extends "base.html.twig" %}

{% block body %}
	{% set appName = 'Cauris-iT' %}
	<!-- <body data-layout="horizontal"> -->
	<div class="auth-page">
		<div class="container-fluid p-0">
			<div class="row g-0">
				<div class="col-xxl-3 col-lg-4 col-md-5">
					<div class="auth-full-page-content d-flex p-sm-5 p-4">
						<div class="w-100">
							<div class="d-flex flex-column h-100">
								<div class="mb-sm-5 mb-0 text-center">
									<a href="{{path('dashboard')}}" class="d-block auth-logo">
										<img src="{{asset('assets/images/cauris-it-beta2.png')}}" alt="" height="40">
									</a>
									<span class="logo-txt">{{appName}}</span>
								</div>
								<div class="auth-content my-auto">
									{% block page_content %}{% endblock %}
								</div>
								<div class="mt-sm-5 mt-0 text-center">
									<p class="mb-0">©
										<script>
											var year = new Date().getFullYear()
											document.write(year==2022 ? year : "2022 - " + year)
										</script>
										{# {{appName}}. Conçu avec
										<i class="mdi mdi-heart text-danger"></i>
										par <a href="#!" class="text-decoration-underline">alladeson</a></p> #}
								</div>
							</div>
						</div>
					</div>
				</div>
				<!-- end auth full page content -->
				<!-- end col -->
				{{include('auth/carousel.html.twig')}}
				<!-- end col -->
			</div>
			<!-- end row -->
		</div>
		<!-- end container fluid -->
	</div>

{% endblock %}
